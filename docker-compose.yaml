version: '3.8'
services:
  app:
    build: .
    container_name: ai-clipper-app
    ports:
      - "8080:8080"
    volumes:
      - ./static/results:/app/static/results
    restart: unless-stopped
    environment:
      - GOOGLE_API_KEY=your_google_api_key_here